You are doing unattended overnight development on a hobby game project called Potluck, in this git repository (working directory: /home/vagrant/potluck). This is one of many nightly automated sessions in a chain — no human is watching this session, you will not get to ask questions, and the next session will have zero memory of this one beyond whatever is on disk (the current files, including any uncommitted changes from tonight or earlier nights the user hasn't reviewed yet) plus PROGRESS.md and git history for anything already committed. Work autonomously and use good judgment.

First, read in this order: DESIGN.md (the design bible — authoritative for scope, style, and what NOT to build yet), PROGRESS.md (the running log — read at least the most recent 2-3 entries to know where things stand), and TASKS.md (the current backlog, in build order, with its own rules for how to work the list — re-read those rules, they matter).

You're running inside a VM (not the bare cloud sandbox earlier sessions may have used), so unlike before you now have: a real Chrome browser (google-chrome-stable, headless-capable) for actual screenshot-based visual checks, and Blender (headless via `blender --background --python script.py`) for real model authoring — see DESIGN.md's Art bible, both code-built primitives and Blender-authored glTF models are fully approved production paths, use whichever fits the prop. Chrome upgrades your ability to verify player-visible behavior actually looks right instead of just reading code, which TASKS.md already asks you to do "as well as you can."

Then:
1. Take the first unchecked task in TASKS.md and implement it. If there's clearly good time left in the session after finishing it cleanly, move on to the next unchecked task(s) too — don't stop after one if there's more room, but don't rush sloppy work just to check more boxes.
2. Follow TASKS.md's own completion criteria (build must still run; use the real browser now available to sanity-check visual/behavioral changes when that's the relevant kind of check).
3. Before finishing, no matter how much or little got done: run `npm install && npm run build` from the repo root and confirm it succeeds — this is a hard requirement, never leave the tree in a broken/non-building state. Update TASKS.md checkboxes to reflect reality, and add new sub-tasks there if you discover the work is bigger than one task implied. Append exactly one new entry to the TOP of PROGRESS.md using the template already at the top of that file.
4. Do NOT run `git commit`, `git add`, or `git push` — leave every change in the working tree. This repo directory is a live synced folder with the host machine, so the host sees your changes immediately; the user reviews and commits/pushes it themselves each morning. This is different from earlier cloud-sandbox sessions, which had to commit+push because their sandbox was disposable — this VM's disk (and the shared repo folder) persists, so that's no longer needed or wanted.
5. Respect DESIGN.md's non-goals: no hosting/deployment setup, no database, no accounts/auth, no voice chat. Do not start Special #2 (Hot Potato Dash) before Special #1 (Recipe Roulette) is fully playable end-to-end per TASKS.md — depth over breadth.

If you hit a genuine design ambiguity DESIGN.md doesn't resolve, don't stall on it — pick the simplest interpretation consistent with DESIGN.md, write the assumption down in PROGRESS.md, and keep moving. Leave the repo in a state strictly better and more complete than you found it, buildable, and clear enough for a session with no memory of this one to pick up cleanly tomorrow night.
